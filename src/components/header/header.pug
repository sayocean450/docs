include ../icon/icon

mixin header(activePage, modeValue='inactive')
    include ../icon/icon
    div.header(class=`header`)
        div.header__logo
            a.header__logo-link(href="index.html") cошников
        div.header__menu
            if activePage === 'writing'
                a.header__menu-elem.header__menu-elem_active(href="stories.html") рассказы
                a.header__menu-elem(href="autofiction-esse.html") автофикшн-эссе
                a.header__menu-elem(href="contacts.html") контакты
            else if activePage === 'contacts'
                a.header__menu-elem(href="stories.html") рассказы
                a.header__menu-elem(href="autofiction-esse.html") автофикшн-эссе
                a.header__menu-elem.header__menu-elem_active(href="contacts.html") контакты
            else if activePage === 'autofiction-esse'
                a.header__menu-elem(href="stories.html") рассказы
                a.header__menu-elem.header__menu-elem_active(href="autofiction-esse.html") автофикшн-эссе
                a.header__menu-elem(href="contacts.html") контакты
            else
                a.header__menu-elem(href="stories.html") рассказы
                a.header__menu-elem(href="autofiction-esse.html") автофикшн-эссе
                a.header__menu-elem(href="contacts.html") контакты
        div.header__menu_mobile#modeValue
            if modeValue === 'active'
                a(href="javascript:history.back()")
                    +iconClose('24px').icon_blue
            else
                a(href="menu.html")
                    +iconMenu('24px').icon_blue
